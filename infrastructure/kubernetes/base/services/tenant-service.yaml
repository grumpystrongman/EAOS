apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: openaegis
  labels:
    app: tenant-service
spec:
  selector:
    app: tenant-service
  ports:
    - name: http
      port: 3002
      targetPort: http
