apiVersion: v1
kind: Service
metadata:
  name: workflow-orchestrator
  namespace: openaegis
  labels:
    app: workflow-orchestrator
spec:
  selector:
    app: workflow-orchestrator
  ports:
    - name: http
      port: 3004
      targetPort: http
