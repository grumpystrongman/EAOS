apiVersion: v1
kind: Service
metadata:
  name: agent-registry
  namespace: openaegis
  labels:
    app: agent-registry
spec:
  selector:
    app: agent-registry
  ports:
    - name: http
      port: 3005
      targetPort: http
