apiVersion: v1
kind: Service
metadata:
  name: tool-registry
  namespace: openaegis
  labels:
    app: tool-registry
spec:
  selector:
    app: tool-registry
  ports:
    - name: http
      port: 3006
      targetPort: http
